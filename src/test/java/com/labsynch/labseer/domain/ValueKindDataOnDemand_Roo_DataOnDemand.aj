// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ValueKind;
import com.labsynch.labseer.domain.ValueKindDataOnDemand;
import com.labsynch.labseer.domain.ValueType;
import com.labsynch.labseer.domain.ValueTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ValueKindDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ValueKindDataOnDemand: @Component;
    
    private Random ValueKindDataOnDemand.rnd = new SecureRandom();
    
    private List<ValueKind> ValueKindDataOnDemand.data;
    
    @Autowired
    ValueTypeDataOnDemand ValueKindDataOnDemand.valueTypeDataOnDemand;
    
    public ValueKind ValueKindDataOnDemand.getNewTransientValueKind(int index) {
        ValueKind obj = new ValueKind();
        setKindName(obj, index);
        setLsType(obj, index);
        setLsTypeAndKind(obj, index);
        return obj;
    }
    
    public void ValueKindDataOnDemand.setKindName(ValueKind obj, int index) {
        String kindName = "kindName_" + index;
        if (kindName.length() > 64) {
            kindName = kindName.substring(0, 64);
        }
        obj.setKindName(kindName);
    }
    
    public void ValueKindDataOnDemand.setLsType(ValueKind obj, int index) {
        ValueType lsType = valueTypeDataOnDemand.getRandomValueType();
        obj.setLsType(lsType);
    }
    
    public void ValueKindDataOnDemand.setLsTypeAndKind(ValueKind obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = new Random().nextInt(10) + lsTypeAndKind.substring(1, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public ValueKind ValueKindDataOnDemand.getSpecificValueKind(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ValueKind obj = data.get(index);
        Long id = obj.getId();
        return ValueKind.findValueKind(id);
    }
    
    public ValueKind ValueKindDataOnDemand.getRandomValueKind() {
        init();
        ValueKind obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ValueKind.findValueKind(id);
    }
    
    public boolean ValueKindDataOnDemand.modifyValueKind(ValueKind obj) {
        return false;
    }
    
    public void ValueKindDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ValueKind.findValueKindEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ValueKind' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ValueKind>();
        for (int i = 0; i < 10; i++) {
            ValueKind obj = getNewTransientValueKind(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
