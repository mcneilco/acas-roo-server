// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ProtocolKind;
import com.labsynch.labseer.domain.ProtocolKindDataOnDemand;
import com.labsynch.labseer.domain.ProtocolKindIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProtocolKindIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProtocolKindIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProtocolKindIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProtocolKindIntegrationTest: @Transactional;
    
    @Autowired
    ProtocolKindDataOnDemand ProtocolKindIntegrationTest.dod;
    
    @Test
    public void ProtocolKindIntegrationTest.testCountProtocolKinds() {
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", dod.getRandomProtocolKind());
        long count = ProtocolKind.countProtocolKinds();
        Assert.assertTrue("Counter for 'ProtocolKind' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testFindProtocolKind() {
        ProtocolKind obj = dod.getRandomProtocolKind();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to provide an identifier", id);
        obj = ProtocolKind.findProtocolKind(id);
        Assert.assertNotNull("Find method for 'ProtocolKind' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProtocolKind' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testFindAllProtocolKinds() {
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", dod.getRandomProtocolKind());
        long count = ProtocolKind.countProtocolKinds();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProtocolKind', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProtocolKind> result = ProtocolKind.findAllProtocolKinds();
        Assert.assertNotNull("Find all method for 'ProtocolKind' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProtocolKind' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testFindProtocolKindEntries() {
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", dod.getRandomProtocolKind());
        long count = ProtocolKind.countProtocolKinds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProtocolKind> result = ProtocolKind.findProtocolKindEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProtocolKind' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProtocolKind' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testFlush() {
        ProtocolKind obj = dod.getRandomProtocolKind();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to provide an identifier", id);
        obj = ProtocolKind.findProtocolKind(id);
        Assert.assertNotNull("Find method for 'ProtocolKind' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProtocolKind(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProtocolKind' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testMergeUpdate() {
        ProtocolKind obj = dod.getRandomProtocolKind();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to provide an identifier", id);
        obj = ProtocolKind.findProtocolKind(id);
        boolean modified =  dod.modifyProtocolKind(obj);
        Integer currentVersion = obj.getVersion();
        ProtocolKind merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProtocolKind' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", dod.getRandomProtocolKind());
        ProtocolKind obj = dod.getNewTransientProtocolKind(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProtocolKind' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ProtocolKind' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProtocolKindIntegrationTest.testRemove() {
        ProtocolKind obj = dod.getRandomProtocolKind();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolKind' failed to provide an identifier", id);
        obj = ProtocolKind.findProtocolKind(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProtocolKind' with identifier '" + id + "'", ProtocolKind.findProtocolKind(id));
    }
    
}
