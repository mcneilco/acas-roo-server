// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.Protocol;
import com.labsynch.labseer.domain.ProtocolDataOnDemand;
import com.labsynch.labseer.domain.ProtocolIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProtocolIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProtocolIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProtocolIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProtocolIntegrationTest: @Transactional;
    
    @Autowired
    ProtocolDataOnDemand ProtocolIntegrationTest.dod;
    
    @Test
    public void ProtocolIntegrationTest.testCountProtocols() {
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", dod.getRandomProtocol());
        long count = Protocol.countProtocols();
        Assert.assertTrue("Counter for 'Protocol' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProtocolIntegrationTest.testFindProtocol() {
        Protocol obj = dod.getRandomProtocol();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to provide an identifier", id);
        obj = Protocol.findProtocol(id);
        Assert.assertNotNull("Find method for 'Protocol' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Protocol' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProtocolIntegrationTest.testFindAllProtocols() {
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", dod.getRandomProtocol());
        long count = Protocol.countProtocols();
        Assert.assertTrue("Too expensive to perform a find all test for 'Protocol', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Protocol> result = Protocol.findAllProtocols();
        Assert.assertNotNull("Find all method for 'Protocol' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Protocol' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProtocolIntegrationTest.testFindProtocolEntries() {
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", dod.getRandomProtocol());
        long count = Protocol.countProtocols();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Protocol> result = Protocol.findProtocolEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Protocol' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Protocol' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProtocolIntegrationTest.testFlush() {
        Protocol obj = dod.getRandomProtocol();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to provide an identifier", id);
        obj = Protocol.findProtocol(id);
        Assert.assertNotNull("Find method for 'Protocol' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProtocol(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Protocol' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolIntegrationTest.testMergeUpdate() {
        Protocol obj = dod.getRandomProtocol();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to provide an identifier", id);
        obj = Protocol.findProtocol(id);
        boolean modified =  dod.modifyProtocol(obj);
        Integer currentVersion = obj.getVersion();
        Protocol merged = (Protocol)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Protocol' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", dod.getRandomProtocol());
        Protocol obj = dod.getNewTransientProtocol(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Protocol' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Protocol' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Protocol' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProtocolIntegrationTest.testRemove() {
        Protocol obj = dod.getRandomProtocol();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Protocol' failed to provide an identifier", id);
        obj = Protocol.findProtocol(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Protocol' with identifier '" + id + "'", Protocol.findProtocol(id));
    }
    
}
