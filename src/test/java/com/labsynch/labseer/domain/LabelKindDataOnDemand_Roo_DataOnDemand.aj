// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.LabelKind;
import com.labsynch.labseer.domain.LabelKindDataOnDemand;
import com.labsynch.labseer.domain.LabelTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect LabelKindDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LabelKindDataOnDemand: @Component;
    
    private Random LabelKindDataOnDemand.rnd = new SecureRandom();
    
    private List<LabelKind> LabelKindDataOnDemand.data;
    
    @Autowired
    LabelTypeDataOnDemand LabelKindDataOnDemand.labelTypeDataOnDemand;
    
    public LabelKind LabelKindDataOnDemand.getNewTransientLabelKind(int index) {
        LabelKind obj = new LabelKind();
        setKindName(obj, index);
        setLsTypeAndKind(obj, index);
        return obj;
    }
    
    public void LabelKindDataOnDemand.setKindName(LabelKind obj, int index) {
        String kindName = "kindName_" + index;
        if (kindName.length() > 255) {
            kindName = kindName.substring(0, 255);
        }
        obj.setKindName(kindName);
    }
    
    public void LabelKindDataOnDemand.setLsTypeAndKind(LabelKind obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = new Random().nextInt(10) + lsTypeAndKind.substring(1, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public LabelKind LabelKindDataOnDemand.getSpecificLabelKind(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        LabelKind obj = data.get(index);
        Long id = obj.getId();
        return LabelKind.findLabelKind(id);
    }
    
    public LabelKind LabelKindDataOnDemand.getRandomLabelKind() {
        init();
        LabelKind obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return LabelKind.findLabelKind(id);
    }
    
    public boolean LabelKindDataOnDemand.modifyLabelKind(LabelKind obj) {
        return false;
    }
    
    public void LabelKindDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = LabelKind.findLabelKindEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'LabelKind' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<LabelKind>();
        for (int i = 0; i < 10; i++) {
            LabelKind obj = getNewTransientLabelKind(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
