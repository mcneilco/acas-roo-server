// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.Container;
import com.labsynch.labseer.domain.ContainerDataOnDemand;
import com.labsynch.labseer.domain.ItxSubjectContainer;
import com.labsynch.labseer.domain.ItxSubjectContainerDataOnDemand;
import com.labsynch.labseer.domain.Subject;
import com.labsynch.labseer.domain.SubjectDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ItxSubjectContainerDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ItxSubjectContainerDataOnDemand: @Component;
    
    private Random ItxSubjectContainerDataOnDemand.rnd = new SecureRandom();
    
    private List<ItxSubjectContainer> ItxSubjectContainerDataOnDemand.data;
    
    @Autowired
    ContainerDataOnDemand ItxSubjectContainerDataOnDemand.containerDataOnDemand;
    
    @Autowired
    SubjectDataOnDemand ItxSubjectContainerDataOnDemand.subjectDataOnDemand;
    
    public ItxSubjectContainer ItxSubjectContainerDataOnDemand.getNewTransientItxSubjectContainer(int index) {
        ItxSubjectContainer obj = new ItxSubjectContainer();
        setCodeName(obj, index);
        setContainer(obj, index);
        setDeleted(obj, index);
        setIgnored(obj, index);
        setLsKind(obj, index);
        setLsTransaction(obj, index);
        setLsType(obj, index);
        setLsTypeAndKind(obj, index);
        setModifiedBy(obj, index);
        setModifiedDate(obj, index);
        setRecordedBy(obj, index);
        setRecordedDate(obj, index);
        setSubject(obj, index);
        return obj;
    }
    
    public void ItxSubjectContainerDataOnDemand.setCodeName(ItxSubjectContainer obj, int index) {
        String codeName = "codeName_" + index;
        if (codeName.length() > 255) {
            codeName = new Random().nextInt(10) + codeName.substring(1, 255);
        }
        obj.setCodeName(codeName);
    }
    
    public void ItxSubjectContainerDataOnDemand.setContainer(ItxSubjectContainer obj, int index) {
        Container container = containerDataOnDemand.getRandomContainer();
        obj.setContainer(container);
    }
    
    public void ItxSubjectContainerDataOnDemand.setDeleted(ItxSubjectContainer obj, int index) {
        Boolean deleted = true;
        obj.setDeleted(deleted);
    }
    
    public void ItxSubjectContainerDataOnDemand.setIgnored(ItxSubjectContainer obj, int index) {
        Boolean ignored = true;
        obj.setIgnored(ignored);
    }
    
    public void ItxSubjectContainerDataOnDemand.setLsKind(ItxSubjectContainer obj, int index) {
        String lsKind = "lsKind_" + index;
        if (lsKind.length() > 255) {
            lsKind = lsKind.substring(0, 255);
        }
        obj.setLsKind(lsKind);
    }
    
    public void ItxSubjectContainerDataOnDemand.setLsTransaction(ItxSubjectContainer obj, int index) {
        Long lsTransaction = new Integer(index).longValue();
        obj.setLsTransaction(lsTransaction);
    }
    
    public void ItxSubjectContainerDataOnDemand.setLsType(ItxSubjectContainer obj, int index) {
        String lsType = "lsType_" + index;
        if (lsType.length() > 255) {
            lsType = lsType.substring(0, 255);
        }
        obj.setLsType(lsType);
    }
    
    public void ItxSubjectContainerDataOnDemand.setLsTypeAndKind(ItxSubjectContainer obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = lsTypeAndKind.substring(0, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public void ItxSubjectContainerDataOnDemand.setModifiedBy(ItxSubjectContainer obj, int index) {
        String modifiedBy = "modifiedBy_" + index;
        if (modifiedBy.length() > 255) {
            modifiedBy = modifiedBy.substring(0, 255);
        }
        obj.setModifiedBy(modifiedBy);
    }
    
    public void ItxSubjectContainerDataOnDemand.setModifiedDate(ItxSubjectContainer obj, int index) {
        Date modifiedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setModifiedDate(modifiedDate);
    }
    
    public void ItxSubjectContainerDataOnDemand.setRecordedBy(ItxSubjectContainer obj, int index) {
        String recordedBy = "recordedBy_" + index;
        if (recordedBy.length() > 255) {
            recordedBy = recordedBy.substring(0, 255);
        }
        obj.setRecordedBy(recordedBy);
    }
    
    public void ItxSubjectContainerDataOnDemand.setRecordedDate(ItxSubjectContainer obj, int index) {
        Date recordedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setRecordedDate(recordedDate);
    }
    
    public void ItxSubjectContainerDataOnDemand.setSubject(ItxSubjectContainer obj, int index) {
        Subject subject = subjectDataOnDemand.getRandomSubject();
        obj.setSubject(subject);
    }
    
    public ItxSubjectContainer ItxSubjectContainerDataOnDemand.getSpecificItxSubjectContainer(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ItxSubjectContainer obj = data.get(index);
        Long id = obj.getId();
        return ItxSubjectContainer.findItxSubjectContainer(id);
    }
    
    public ItxSubjectContainer ItxSubjectContainerDataOnDemand.getRandomItxSubjectContainer() {
        init();
        ItxSubjectContainer obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ItxSubjectContainer.findItxSubjectContainer(id);
    }
    
    public boolean ItxSubjectContainerDataOnDemand.modifyItxSubjectContainer(ItxSubjectContainer obj) {
        return false;
    }
    
    public void ItxSubjectContainerDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ItxSubjectContainer.findItxSubjectContainerEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ItxSubjectContainer' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ItxSubjectContainer>();
        for (int i = 0; i < 10; i++) {
            ItxSubjectContainer obj = getNewTransientItxSubjectContainer(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
