// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.UnitType;
import com.labsynch.labseer.domain.UnitTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UnitTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UnitTypeDataOnDemand: @Component;
    
    private Random UnitTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<UnitType> UnitTypeDataOnDemand.data;
    
    public UnitType UnitTypeDataOnDemand.getNewTransientUnitType(int index) {
        UnitType obj = new UnitType();
        setTypeName(obj, index);
        return obj;
    }
    
    public void UnitTypeDataOnDemand.setTypeName(UnitType obj, int index) {
        String typeName = "typeName_" + index;
        if (typeName.length() > 64) {
            typeName = new Random().nextInt(10) + typeName.substring(1, 64);
        }
        obj.setTypeName(typeName);
    }
    
    public UnitType UnitTypeDataOnDemand.getSpecificUnitType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UnitType obj = data.get(index);
        Long id = obj.getId();
        return UnitType.findUnitType(id);
    }
    
    public UnitType UnitTypeDataOnDemand.getRandomUnitType() {
        init();
        UnitType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return UnitType.findUnitType(id);
    }
    
    public boolean UnitTypeDataOnDemand.modifyUnitType(UnitType obj) {
        return false;
    }
    
    public void UnitTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UnitType.findUnitTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UnitType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UnitType>();
        for (int i = 0; i < 10; i++) {
            UnitType obj = getNewTransientUnitType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
