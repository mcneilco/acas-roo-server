// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ContainerLabel;
import com.labsynch.labseer.domain.ContainerLabelDataOnDemand;
import com.labsynch.labseer.domain.ContainerLabelIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ContainerLabelIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ContainerLabelIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ContainerLabelIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ContainerLabelIntegrationTest: @Transactional;
    
    @Autowired
    ContainerLabelDataOnDemand ContainerLabelIntegrationTest.dod;
    
    @Test
    public void ContainerLabelIntegrationTest.testCountContainerLabels() {
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", dod.getRandomContainerLabel());
        long count = ContainerLabel.countContainerLabels();
        Assert.assertTrue("Counter for 'ContainerLabel' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testFindContainerLabel() {
        ContainerLabel obj = dod.getRandomContainerLabel();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to provide an identifier", id);
        obj = ContainerLabel.findContainerLabel(id);
        Assert.assertNotNull("Find method for 'ContainerLabel' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ContainerLabel' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testFindAllContainerLabels() {
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", dod.getRandomContainerLabel());
        long count = ContainerLabel.countContainerLabels();
        Assert.assertTrue("Too expensive to perform a find all test for 'ContainerLabel', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ContainerLabel> result = ContainerLabel.findAllContainerLabels();
        Assert.assertNotNull("Find all method for 'ContainerLabel' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ContainerLabel' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testFindContainerLabelEntries() {
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", dod.getRandomContainerLabel());
        long count = ContainerLabel.countContainerLabels();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ContainerLabel> result = ContainerLabel.findContainerLabelEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ContainerLabel' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ContainerLabel' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testFlush() {
        ContainerLabel obj = dod.getRandomContainerLabel();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to provide an identifier", id);
        obj = ContainerLabel.findContainerLabel(id);
        Assert.assertNotNull("Find method for 'ContainerLabel' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyContainerLabel(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ContainerLabel' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testMergeUpdate() {
        ContainerLabel obj = dod.getRandomContainerLabel();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to provide an identifier", id);
        obj = ContainerLabel.findContainerLabel(id);
        boolean modified =  dod.modifyContainerLabel(obj);
        Integer currentVersion = obj.getVersion();
        ContainerLabel merged = (ContainerLabel)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ContainerLabel' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", dod.getRandomContainerLabel());
        ContainerLabel obj = dod.getNewTransientContainerLabel(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ContainerLabel' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ContainerLabel' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ContainerLabelIntegrationTest.testRemove() {
        ContainerLabel obj = dod.getRandomContainerLabel();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ContainerLabel' failed to provide an identifier", id);
        obj = ContainerLabel.findContainerLabel(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ContainerLabel' with identifier '" + id + "'", ContainerLabel.findContainerLabel(id));
    }
    
}
