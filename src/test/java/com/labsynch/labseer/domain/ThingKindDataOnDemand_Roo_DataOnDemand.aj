// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ThingKind;
import com.labsynch.labseer.domain.ThingKindDataOnDemand;
import com.labsynch.labseer.domain.ThingType;
import com.labsynch.labseer.domain.ThingTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ThingKindDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ThingKindDataOnDemand: @Component;
    
    private Random ThingKindDataOnDemand.rnd = new SecureRandom();
    
    private List<ThingKind> ThingKindDataOnDemand.data;
    
    @Autowired
    ThingTypeDataOnDemand ThingKindDataOnDemand.thingTypeDataOnDemand;
    
    public ThingKind ThingKindDataOnDemand.getNewTransientThingKind(int index) {
        ThingKind obj = new ThingKind();
        setKindName(obj, index);
        setLsType(obj, index);
        setLsTypeAndKind(obj, index);
        return obj;
    }
    
    public void ThingKindDataOnDemand.setKindName(ThingKind obj, int index) {
        String kindName = "kindName_" + index;
        if (kindName.length() > 255) {
            kindName = kindName.substring(0, 255);
        }
        obj.setKindName(kindName);
    }
    
    public void ThingKindDataOnDemand.setLsType(ThingKind obj, int index) {
        ThingType lsType = thingTypeDataOnDemand.getRandomThingType();
        obj.setLsType(lsType);
    }
    
    public void ThingKindDataOnDemand.setLsTypeAndKind(ThingKind obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = new Random().nextInt(10) + lsTypeAndKind.substring(1, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public ThingKind ThingKindDataOnDemand.getSpecificThingKind(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ThingKind obj = data.get(index);
        Long id = obj.getId();
        return ThingKind.findThingKind(id);
    }
    
    public ThingKind ThingKindDataOnDemand.getRandomThingKind() {
        init();
        ThingKind obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ThingKind.findThingKind(id);
    }
    
    public boolean ThingKindDataOnDemand.modifyThingKind(ThingKind obj) {
        return false;
    }
    
    public void ThingKindDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ThingKind.findThingKindEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ThingKind' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ThingKind>();
        for (int i = 0; i < 10; i++) {
            ThingKind obj = getNewTransientThingKind(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
