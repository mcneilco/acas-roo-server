// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ValueKind;
import com.labsynch.labseer.domain.ValueKindDataOnDemand;
import com.labsynch.labseer.domain.ValueKindIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ValueKindIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ValueKindIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ValueKindIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ValueKindIntegrationTest: @Transactional;
    
    @Autowired
    ValueKindDataOnDemand ValueKindIntegrationTest.dod;
    
    @Test
    public void ValueKindIntegrationTest.testCountValueKinds() {
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", dod.getRandomValueKind());
        long count = ValueKind.countValueKinds();
        Assert.assertTrue("Counter for 'ValueKind' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ValueKindIntegrationTest.testFindValueKind() {
        ValueKind obj = dod.getRandomValueKind();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to provide an identifier", id);
        obj = ValueKind.findValueKind(id);
        Assert.assertNotNull("Find method for 'ValueKind' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ValueKind' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ValueKindIntegrationTest.testFindAllValueKinds() {
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", dod.getRandomValueKind());
        long count = ValueKind.countValueKinds();
        Assert.assertTrue("Too expensive to perform a find all test for 'ValueKind', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ValueKind> result = ValueKind.findAllValueKinds();
        Assert.assertNotNull("Find all method for 'ValueKind' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ValueKind' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ValueKindIntegrationTest.testFindValueKindEntries() {
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", dod.getRandomValueKind());
        long count = ValueKind.countValueKinds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ValueKind> result = ValueKind.findValueKindEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ValueKind' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ValueKind' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ValueKindIntegrationTest.testFlush() {
        ValueKind obj = dod.getRandomValueKind();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to provide an identifier", id);
        obj = ValueKind.findValueKind(id);
        Assert.assertNotNull("Find method for 'ValueKind' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyValueKind(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ValueKind' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ValueKindIntegrationTest.testMergeUpdate() {
        ValueKind obj = dod.getRandomValueKind();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to provide an identifier", id);
        obj = ValueKind.findValueKind(id);
        boolean modified =  dod.modifyValueKind(obj);
        Integer currentVersion = obj.getVersion();
        ValueKind merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ValueKind' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ValueKindIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", dod.getRandomValueKind());
        ValueKind obj = dod.getNewTransientValueKind(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ValueKind' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ValueKind' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ValueKindIntegrationTest.testRemove() {
        ValueKind obj = dod.getRandomValueKind();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ValueKind' failed to provide an identifier", id);
        obj = ValueKind.findValueKind(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ValueKind' with identifier '" + id + "'", ValueKind.findValueKind(id));
    }
    
}
