// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ProtocolValue;
import com.labsynch.labseer.domain.ProtocolValueDataOnDemand;
import com.labsynch.labseer.domain.ProtocolValueIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProtocolValueIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProtocolValueIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProtocolValueIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProtocolValueIntegrationTest: @Transactional;
    
    @Autowired
    ProtocolValueDataOnDemand ProtocolValueIntegrationTest.dod;
    
    @Test
    public void ProtocolValueIntegrationTest.testCountProtocolValues() {
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", dod.getRandomProtocolValue());
        long count = ProtocolValue.countProtocolValues();
        Assert.assertTrue("Counter for 'ProtocolValue' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testFindProtocolValue() {
        ProtocolValue obj = dod.getRandomProtocolValue();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to provide an identifier", id);
        obj = ProtocolValue.findProtocolValue(id);
        Assert.assertNotNull("Find method for 'ProtocolValue' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProtocolValue' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testFindAllProtocolValues() {
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", dod.getRandomProtocolValue());
        long count = ProtocolValue.countProtocolValues();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProtocolValue', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProtocolValue> result = ProtocolValue.findAllProtocolValues();
        Assert.assertNotNull("Find all method for 'ProtocolValue' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProtocolValue' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testFindProtocolValueEntries() {
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", dod.getRandomProtocolValue());
        long count = ProtocolValue.countProtocolValues();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProtocolValue> result = ProtocolValue.findProtocolValueEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProtocolValue' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProtocolValue' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testFlush() {
        ProtocolValue obj = dod.getRandomProtocolValue();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to provide an identifier", id);
        obj = ProtocolValue.findProtocolValue(id);
        Assert.assertNotNull("Find method for 'ProtocolValue' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProtocolValue(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProtocolValue' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testMergeUpdate() {
        ProtocolValue obj = dod.getRandomProtocolValue();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to provide an identifier", id);
        obj = ProtocolValue.findProtocolValue(id);
        boolean modified =  dod.modifyProtocolValue(obj);
        Integer currentVersion = obj.getVersion();
        ProtocolValue merged = (ProtocolValue)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProtocolValue' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", dod.getRandomProtocolValue());
        ProtocolValue obj = dod.getNewTransientProtocolValue(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProtocolValue' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ProtocolValue' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProtocolValueIntegrationTest.testRemove() {
        ProtocolValue obj = dod.getRandomProtocolValue();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProtocolValue' failed to provide an identifier", id);
        obj = ProtocolValue.findProtocolValue(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProtocolValue' with identifier '" + id + "'", ProtocolValue.findProtocolValue(id));
    }
    
}
