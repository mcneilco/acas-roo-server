// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.ContainerKind;
import com.labsynch.labseer.domain.ContainerKindDataOnDemand;
import com.labsynch.labseer.domain.ContainerType;
import com.labsynch.labseer.domain.ContainerTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ContainerKindDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ContainerKindDataOnDemand: @Component;
    
    private Random ContainerKindDataOnDemand.rnd = new SecureRandom();
    
    private List<ContainerKind> ContainerKindDataOnDemand.data;
    
    @Autowired
    ContainerTypeDataOnDemand ContainerKindDataOnDemand.containerTypeDataOnDemand;
    
    public ContainerKind ContainerKindDataOnDemand.getNewTransientContainerKind(int index) {
        ContainerKind obj = new ContainerKind();
        setKindName(obj, index);
        setLsType(obj, index);
        setLsTypeAndKind(obj, index);
        return obj;
    }
    
    public void ContainerKindDataOnDemand.setKindName(ContainerKind obj, int index) {
        String kindName = "kindName_" + index;
        if (kindName.length() > 255) {
            kindName = kindName.substring(0, 255);
        }
        obj.setKindName(kindName);
    }
    
    public void ContainerKindDataOnDemand.setLsType(ContainerKind obj, int index) {
        ContainerType lsType = containerTypeDataOnDemand.getRandomContainerType();
        obj.setLsType(lsType);
    }
    
    public void ContainerKindDataOnDemand.setLsTypeAndKind(ContainerKind obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = new Random().nextInt(10) + lsTypeAndKind.substring(1, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public ContainerKind ContainerKindDataOnDemand.getSpecificContainerKind(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ContainerKind obj = data.get(index);
        Long id = obj.getId();
        return ContainerKind.findContainerKind(id);
    }
    
    public ContainerKind ContainerKindDataOnDemand.getRandomContainerKind() {
        init();
        ContainerKind obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ContainerKind.findContainerKind(id);
    }
    
    public boolean ContainerKindDataOnDemand.modifyContainerKind(ContainerKind obj) {
        return false;
    }
    
    public void ContainerKindDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ContainerKind.findContainerKindEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ContainerKind' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ContainerKind>();
        for (int i = 0; i < 10; i++) {
            ContainerKind obj = getNewTransientContainerKind(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
