// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.labsynch.labseer.domain;

import com.labsynch.labseer.domain.TreatmentGroup;
import com.labsynch.labseer.domain.TreatmentGroupDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect TreatmentGroupDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TreatmentGroupDataOnDemand: @Component;
    
    private Random TreatmentGroupDataOnDemand.rnd = new SecureRandom();
    
    private List<TreatmentGroup> TreatmentGroupDataOnDemand.data;
    
    public TreatmentGroup TreatmentGroupDataOnDemand.getNewTransientTreatmentGroup(int index) {
        TreatmentGroup obj = new TreatmentGroup();
        setCodeName(obj, index);
        setDeleted(obj, index);
        setIgnored(obj, index);
        setLsKind(obj, index);
        setLsTransaction(obj, index);
        setLsType(obj, index);
        setLsTypeAndKind(obj, index);
        setModifiedBy(obj, index);
        setModifiedDate(obj, index);
        setRecordedBy(obj, index);
        setRecordedDate(obj, index);
        return obj;
    }
    
    public void TreatmentGroupDataOnDemand.setCodeName(TreatmentGroup obj, int index) {
        String codeName = "codeName_" + index;
        if (codeName.length() > 255) {
            codeName = new Random().nextInt(10) + codeName.substring(1, 255);
        }
        obj.setCodeName(codeName);
    }
    
    public void TreatmentGroupDataOnDemand.setDeleted(TreatmentGroup obj, int index) {
        Boolean deleted = true;
        obj.setDeleted(deleted);
    }
    
    public void TreatmentGroupDataOnDemand.setIgnored(TreatmentGroup obj, int index) {
        Boolean ignored = true;
        obj.setIgnored(ignored);
    }
    
    public void TreatmentGroupDataOnDemand.setLsKind(TreatmentGroup obj, int index) {
        String lsKind = "lsKind_" + index;
        if (lsKind.length() > 255) {
            lsKind = lsKind.substring(0, 255);
        }
        obj.setLsKind(lsKind);
    }
    
    public void TreatmentGroupDataOnDemand.setLsTransaction(TreatmentGroup obj, int index) {
        Long lsTransaction = new Integer(index).longValue();
        obj.setLsTransaction(lsTransaction);
    }
    
    public void TreatmentGroupDataOnDemand.setLsType(TreatmentGroup obj, int index) {
        String lsType = "lsType_" + index;
        if (lsType.length() > 255) {
            lsType = lsType.substring(0, 255);
        }
        obj.setLsType(lsType);
    }
    
    public void TreatmentGroupDataOnDemand.setLsTypeAndKind(TreatmentGroup obj, int index) {
        String lsTypeAndKind = "lsTypeAndKind_" + index;
        if (lsTypeAndKind.length() > 255) {
            lsTypeAndKind = lsTypeAndKind.substring(0, 255);
        }
        obj.setLsTypeAndKind(lsTypeAndKind);
    }
    
    public void TreatmentGroupDataOnDemand.setModifiedBy(TreatmentGroup obj, int index) {
        String modifiedBy = "modifiedBy_" + index;
        if (modifiedBy.length() > 255) {
            modifiedBy = modifiedBy.substring(0, 255);
        }
        obj.setModifiedBy(modifiedBy);
    }
    
    public void TreatmentGroupDataOnDemand.setModifiedDate(TreatmentGroup obj, int index) {
        Date modifiedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setModifiedDate(modifiedDate);
    }
    
    public void TreatmentGroupDataOnDemand.setRecordedBy(TreatmentGroup obj, int index) {
        String recordedBy = "recordedBy_" + index;
        if (recordedBy.length() > 255) {
            recordedBy = recordedBy.substring(0, 255);
        }
        obj.setRecordedBy(recordedBy);
    }
    
    public void TreatmentGroupDataOnDemand.setRecordedDate(TreatmentGroup obj, int index) {
        Date recordedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setRecordedDate(recordedDate);
    }
    
    public TreatmentGroup TreatmentGroupDataOnDemand.getSpecificTreatmentGroup(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        TreatmentGroup obj = data.get(index);
        Long id = obj.getId();
        return TreatmentGroup.findTreatmentGroup(id);
    }
    
    public TreatmentGroup TreatmentGroupDataOnDemand.getRandomTreatmentGroup() {
        init();
        TreatmentGroup obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return TreatmentGroup.findTreatmentGroup(id);
    }
    
    public boolean TreatmentGroupDataOnDemand.modifyTreatmentGroup(TreatmentGroup obj) {
        return false;
    }
    
    public void TreatmentGroupDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = TreatmentGroup.findTreatmentGroupEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'TreatmentGroup' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<TreatmentGroup>();
        for (int i = 0; i < 10; i++) {
            TreatmentGroup obj = getNewTransientTreatmentGroup(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
